// Reexportar la instancia de autenticación correcta proporcionada por el paquete shared
import { authApi as auth } from '@repo/auth';

export { auth }; // para compatibilidad con imports existentes
